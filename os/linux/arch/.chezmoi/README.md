# Arch Linux chezmoi source state

Arch Linux uses the shared source directory documented in
`common/.chezmoi/README.md`. The shared `.chezmoiignore.tmpl` lists Linux-only
paths that chezmoi skips on macOS.
